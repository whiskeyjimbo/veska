---
milestone: M3
title: "Pipelines and Embedder"
status: planned
last_reviewed: 2026-05-14
related: [SOLO-08, SOLO-11, SOLO-13, SOLO-14, ADR-S0014, ADR-S0015]
---

# M3 — Pipelines and Embedder

**Goal:** Promotion pipeline runs structural checks synchronously; an
async embedder worker keeps `node_embedding_refs` drained;
`semantic_search` runs against the production vector path (sqlite-vec
default, usearch/float16 above the M2-ratified threshold per
ADR-S0015); auto-link suggestions land;
revalidation sweeps invalidate stale findings on content drift.

**Dependencies:** M1, M2 (incl. m2.06 dual-backend ratification).

## Exit gates

- [ ] Embedder throughput on local CPU Ollama: measured at 5–20 emb/s on the reference laptop.
- [ ] `semantic_search` recall measured at 50k and 250k against the production usearch/float16 backend on a fixed eval set; results recorded in the milestone close report. (The HNSW pivot itself was ratified in M2; ADR-S0014's M1 bench already recorded 0.987@50k / 0.954@250k — M3 confirms the number end-to-end on the live `semantic_search` path.)
- [ ] Auto-link false-positive rate measured on a fixture and surfaced in the milestone close report.
- [ ] Revalidation completes in < 60s for a 10k-edge commit.
- [ ] All four post-promotion queue `work_kind` lanes drain to `state='done'` under a fuzz workload.

## Epics

### Epic m3.01 — Promotion pipeline

**Tracker:** TBD (beads issue created in Stage 2)

**DoD**
- [ ] Structural checks run inside the promotion transaction or immediately after, before hook return.
- [ ] Findings emitted with `source_layer='structural'`.
- [ ] Check set: parse-failure, dead-code (no inbound edges), contract-drift (signature change vs. prior promotion).
- [ ] Per-check timing surfaced in `veska_seal_latency_seconds`.

**Sub-issues**
- `m3.01.1-check-runner` — synchronous check pipeline.
- `m3.01.2-parse-failure` — surface tree-sitter errors as findings.
- `m3.01.3-dead-code` — inbound-edge sweep.
- `m3.01.4-contract-drift` — signature comparison.

### Epic m3.02 — Embedder worker

**Tracker:** TBD (beads issue created in Stage 2)

**DoD**
- [ ] Goroutine drains `node_embedding_refs` where `state='pending'`.
- [ ] Throttled by `embedder.rate_per_sec` config; default 10.
- [ ] Backpressure via `veska_embed_queue_depth`.
- [ ] Failed batches retry up to 3; `state='failed'` after that, surfaced in `veska doctor`.

**Sub-issues**
- `m3.02.1-batch-loop` — pull pending; batch by 32; embed.
- `m3.02.2-rate-limit` — token bucket on requests per second.
- `m3.02.3-error-handling` — retry policy; failed-state surfacing.
- `m3.02.4-content-addressed` — dedupe by `content_hash` before calling Ollama.

### Epic m3.03 — Semantic search live

**Tracker:** TBD (beads issue created in Stage 2)

**DoD**
- [ ] k-NN query path returns ranked node ids in
  `< 100ms p95 k=10` at 50k vectors and `< 150ms p95 k=10` at 250k
  vectors via the dual-backend `VectorStorage` port (sqlite-vec
  default, usearch/float16 above `SQLiteVecYellowThreshold`).
  Numbers already recorded at the port layer in ADR-S0014; this
  epic ships the end-to-end `semantic_search` path against the
  port.
- [ ] `degraded_reasons: ['embedder_unreachable']` when Ollama is
  down; lexical fallback returned.
- [ ] Recall measured against a fixed eval set at 50k and 250k;
  recorded in milestone close.
- [ ] `eng_search_semantic` and `eng_search_similar` MCP tools
  registered (m3.06 owns the registration; this epic owns the
  underlying query path).

**Sub-issues**
- `m3.03.1-knn-query` — k-NN through `VectorStorage` (backend-agnostic).
- `m3.03.2-lexical-fallback` — substring + symbol-name fallback.
- `m3.03.3-eval-harness` — recall measurement against fixture.

### Epic m3.04 — Auto-link

**Tracker:** TBD (beads issue created in Stage 2)

**DoD**
- [ ] post-promotion queue `work_kind='auto_link'` proposes `Edge` rows with `confidence='unresolved'` from embedding similarity AND emits one finding per proposed edge (`source_layer='semantic'`) whose anchor is that specific edge. The edge is never inserted silently — it only becomes visible via the finding.
- [ ] Threshold configurable; default tuned on the eval set.
- [ ] No auto-merge; the developer accepts or suppresses.
- [ ] Accept-flow uses existing `eng_close_finding` with
  `reason="accept"`; the close handler promotes the anchor's
  unresolved edge to `confidence='definite'`. Suppress/close paths
  leave the edge at `confidence='unresolved'` (visible only to
  queries that opt into unresolved edges). No new MCP tool.

**Sub-issues**
- `m3.04.1-similarity` — top-k similarity per node.
- `m3.04.2-finding-shape` — auto-link finding kind.
- `m3.04.3-accept-flow` — `eng_close_finding[reason="accept"]` promotes the linked edge.
- `m3.04.4-fp-measurement` — false-positive rate on fixture.

### Epic m3.05 — Revalidation

**Tracker:** TBD (beads issue created in Stage 2)

**DoD**
- [ ] post-promotion queue `work_kind='revalidate'` re-checks open findings whose anchor `content_hash` changed.
- [ ] Findings transition to `closed` with `closed_reason='revalidated_obsolete'` (rule no longer fires) or `closed_reason='superseded_by_revalidation'` (rule fires on new content; new finding linked back). State stays in `{open, closed}` — no `superseded` state. (SOLO-04 §8.1.)
- [ ] Sweep runs after promotion; bounded to the symbols touched by the promotion.

**Sub-issues**
- `m3.05.1-anchor-tracking` — finding ↔ node hash linkage.
- `m3.05.2-sweep` — revalidate goroutine.
- `m3.05.3-supersede` — chain old → new finding.
- `m3.05.4-bench` — 10k-edge commit revalidation timing.

### Epic m3.06 — MCP tools (search, blast radius, diff, todos)

**Tracker:** TBD (beads issue created in Stage 2)

**DoD**
- [ ] Register 7 MCP tools, names matching SOLO-09 §3:
  `eng_search_semantic`, `eng_search_similar`,
  `eng_get_blast_radius`, `eng_get_dirty_blast_radius`,
  `eng_find_changed_symbols`, `eng_get_diff_blast_radius`,
  `eng_find_todos`.
- [ ] Total tool count after M3: 26 (19 from M2 + 7 from M3).
- [ ] Each tool's underlying machinery is M1/M2/M3 work; this
  epic owns only the MCP registration, descriptions, and
  table-driven handler tests.

**Sub-issues**
- `m3.06.1-search-tools` — `eng_search_semantic`, `eng_search_similar` (depends on m3.03).
- `m3.06.2-blast-radius` — `eng_get_blast_radius`, `eng_get_dirty_blast_radius`.
- `m3.06.3-diff-tools` — `eng_find_changed_symbols`, `eng_get_diff_blast_radius`.
- `m3.06.4-todos` — `eng_find_todos` (depends on M2 active-task tools).
